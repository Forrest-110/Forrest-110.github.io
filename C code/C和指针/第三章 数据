#数据

##3.1基本数据类型
###3.1.1整型
  char 0-127
  signed char -127-127（signed一般只用于char）
  unsigned char 0-255
  short int -32767-32767
  unsigned short int 0-65535
  int -32767-32767
  unsigned int 0-65535
  long int -2147483647-2147483647
  unsigned long int 0-4294967295
  
  limits.h说明了各种整型的特点
  字面值：一种实体，指定了自身的值，并不允许发生改变。
  整型字面值属于哪一种类型取决于它是如何书写的。添加l或L被解释为Long，U或u被解释为unsigned。0开头为八进制，0x开头为16进制。默认情况下是最短但能完整容纳。
  宽字符常量：一个多字节字符常量的前面加上一个L。例如L'杭'。
  
  枚举类型：值为符号常量而不是字面值
  
##3.2基本声明
说明符（一个或多个） 声明表达式列表

说明符包含关键字，用于描述被声明的标识符的基本类型。也可以改变标识符的默认存储类型和作用域。

声明指针：int *b,*c,*d；

隐式声明：如果省略参数类型，会默认他们为整型。

##3.4常量
如何使const一开始有一个值？
法一：const int a=15;初始化
法二：在函数中声明为const的形参在被调用时会得到实参的值。

指针常量
int const *pt;指向整型常量的指针 指针指向的值无法被修改
int *const pi;指向整型的指针常量 指针的值无法被修改
int const *const pt;指针和指针指向的值都无法被修改

##3.5作用域
代码块：位于一对花括号之间的所有语句被称为代码块。
任何在代码块的开始位置声明的标识符都具有代码块作用域。形参也具有代码块作用域。
嵌套的代码块：内层代码块的标识符如果与外层代码块的标识符同名，会隐藏外层标识符。如果函数体内部声明了与形参相同的局部变量，会隐藏形参。
文件作用域：任何在代码块之外声明的标识符都有文件作用域。函数名也具有文件作用域。
原型作用域：只适用于在函数原型中声明的参数名。原型中参数名不是必须的，但如果出现可以起任何名字。
函数作用域：只适用于语句标签，用于goto语句。一个函数中的所有语句标签必须唯一。

##3.6链接属性
当组成一个程序的源文件被编译后，所有的目标文件以及从函数库中引用的函数将链接在一起，形成可执行程序。
然而，如果相同的标识符出现在几个不同的源文件中时，标识符的链接属性决定如何处理不同文件中的标识符。

链接属性共有三种：external,internal,none。
none的标识符总是被当成单独的个体，多个声明被当作不同的独立实体。none链接属性标识符仅允许作用域内访问。
internal标识符在同一个源文件的所有声明中都指同一个实体，不同源文件的声明分属不同的实体。
external标识符不论声明多少次，位于几个源文件，都表示同一个实体。

extern和static用于在声明中修改标识符的链接属性。
如果某个声明是external的，加上static会变成Internal。
static只对默认连接属性为external的声明才有用，若修饰none属性的，改变的是存储类型而非链接属性。
extern为一个标识符指定external链接属性，可以访问在其他任何位置定义的这个实体。

##3.7存储类型
变量的存储类型是指存储变量值的内存类型。
有三个地方可以存储变量：普通内存，运行时堆栈，硬件寄存器。这三个地方的变量具有不同的特性。
变量的默认存储类型取决于它的声明位置。
在任何代码块之外声明的变量总是存储在静态内存中，也就是不属于堆栈的内存。这类变量为静态变量。
静态变量无法指定其他存储类型，在程序运行前创建，在执行期间始终存在，保持原先的值，除非赋值或程序结束。
在代码块内声明的变量的默认存储类型是自动的，存储在堆栈中，称为自动变量。
在程序执行到声明自动变量的代码块时，自动变量被创建。离开代码块自动销毁。如果代码块多次执行，则被多次创建。创建的位置与原先位置可能相同也可能不同。
对于在代码块内部声明的变量，加上static可以变为静态变量。但并不能修改该变量的作用域。
形参不能被声明为静态，因为实参总是在堆栈中传递给函数。
register用于声明自动变量，且储存在寄存器中。
如果把指针声明为寄存器变量，则程序效率能得到提高。
寄存器变量的创建、销毁时间和自动变量相同。但在一个使用寄存器变量的函数返回之前，这些寄存器先前存储的值必须被恢复。机器使用堆栈完成这个任务，把寄存器的内容保存到堆栈中，函数返回时再把值复制会寄存器中。，

静态变量的初始化：可以把想要初始化的值放在变量将会使用的位置。静态变量如果不显式初始化，会被初始化为0。
自动变量初始化：无法判断自动变量的存储位置，因此没有默认的初始值，且显式初始化需要插入隐式的赋值语句。但可以用表达式作为初始化值。



