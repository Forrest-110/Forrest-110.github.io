#第八章 数组
##8.1 一维数组
数组名是指针常量，不能被修改。
只有在两种情况下，数组名不用指针常量来表示。
1、数组名作为sizeof操作符，返回整个数组的长度，而不是指针长度。
2、数组名作为单目操作符&的操作数。

下标引用完全等价与间接访问
int array[10];
int *ap=array+2;

则ap[0]等价于*(ap+0),ap[-1]等价于*(ap-1)
2[array]等价于*(2+array)

指针的效率：
下标不会比指针更有效率，但指针有时会比下标更有效率。
当在数组中每次以固定值移动时，与固定数字相乘的运算在编译时完成，所以在运行时所需的指令就少一些。
声明为寄存器变量的指针通常比位于静态内存和堆栈的指针效率更高
如果可以通过测试一些已经初始化并经过调整的内容来判断循环是否终止，就不需要使用胆大计数器
必须在运行时求值的表达式比常量表达式代价更高

初始化：
静态初始化：静态内存的数组只初始化一次，在程序开始前执行。默认值为0
自动初始化：自动变量位于堆栈，每次进入代码块所处的位置可能不同。默认未定义。
若果不需要每次重新初始化的数组，声明为static

不完整的初始化：
省略的初始化为0

##8.2 多维数组
存储顺序：
按照最右边的下标率先变化的原则

数组名：
matrix[3][10]
matrix是指向一个包含10个元素的数组
matrix+1指向下一行

指向数组的指针
int (*p)[10];
如果打算执行指针运算，应该避免
int (*p)[]=matrix;
数组的长度不见了，则当某个整数与这种类型的指针执行指针运算时，它的值会根据空数组的长度调整

作为函数参数的多维数组：
void func(int mat[][10]);
或
void func(int (*mat)[10]);

初始化：
{100,200,300,400}
等价于
{{100,200}，{300,400}}
但后一种方法对不完整的初始化列表有用。每个子初始列表都可以省略尾部的初始值，每一维的初始列表各自都是一个初始化列表。

数组长度自动计算：
只有第一维能自动给出，第二维必须手动输入

