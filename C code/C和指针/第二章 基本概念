#基本概念
##2.1环境
翻译环境：源代码被转换为可执行的机器指令
执行环境：实际执行代码
这两种环境不必位于同一台机器上

###2.1.1翻译
翻译阶段：组成一个程序的源文件通过编译过程转换为目标代码，然后各个目标文件由链接器捆绑在一起，形成一个单一而完整的可执行程序。链接器也会引入标准C函数库中被该程序所用到的函数。
编译过程：首先是预处理器处理，在源代码上执行文本操作，例如#define,#include。然后，源代码经过解析，判断语句的意思。该过程产生绝大多数错误与警告信息。随后产生目标代码。
目标代码是机器指令的初步形式，用于实现程序。
如果在命令行中加入优化选项，则优化器就会对目标代码处理，使之效率更高，但优化过程需要额外的时间。
1、文件名约定 .c保存源代码，.h是头文件。目标文件.o
2、编译和链接
   1、cc program.c 
   编译并链接一个完全包含于源文件的C程序。产生a.out可执行程序，中间会产生program.o目标文件，但会被删除。
   2、cc main.c sort.c lookup.c
   同时编译链接多个文件，目标文件不会被删除。
   3、cc main.c lookup.o sort.c
   编译一个文件并把它和现存的目标文件连接在一起。
   4、cc -c program.c
   编译并产生目标文件，以后再链接
   5、cc -c  main.c sort.c lookup.c
   编译并产生多个目标文件、以后再链接
   6、cc main.o sort.o lookup.o
   链接几个目标文件
   -o name会把可执行程序保存在name文件中
   默认情况下，链接器在C标准库中查找，如果在编译时加上-lname，则就会同时在name函数库中查找。
3、执行
   首先，程序必须载入到内存中。尚未初始化的变量将在此时得到初始值。
   然后开始执行。一个小型的启动程序与程序链接在一起，负责处理日常事务，例如收集命令行参数。
   接着调用main函数，开始执行程序代码。程序将使用一个运行时堆栈，存储函数的局部变量和返回地址。程序同时可以使用静态内存，存储静态变量。
   最后一个阶段是终止。正常终止是main函数返回或执行了exit。除此之外可能是手动终止、错误终止。

##2.2词法规则
三字母词：几个字符的序列，合起来表示另一个字符。
??( [ ??) ] ??! |
??< { ??> } ??' ^
??= # ??/ \ ??- ~
例：printf(" (are you really sure??):");

\ddd ddd表示八进制数字，表示的字符就是八进制数值代表的字符（ASCII）
\xddd 十六进制

注释/**/如果出现在字符串字面量内，就不再起注释作用。
注释会被预处理器用空格代替，因此，注释可以出现在任何空格可以出现的地方。

除了预处理指令外，一行可以出现多少语句，什么地方应该留下空白，留下多少空白没有被规定。但相邻的标记（单独的字符片段，例如int x）之间必须出现至少一个空白。
y=  x+ 1;
y=
x
+
1;
都是合法的。

一个C程序可以保存于多个源文件中。虽然一个源文件可以包含一个以上的函数，但每个函数都必须完整出现在同一个源文件中。

##问题
1、注释不允许嵌套，/*到第一个*/就结束
4、\40的值是空格，\100的值是@

PS：C语言代码经过编译以后，并没有生成最终的可执行文件（.exe 文件），而是生成了一种叫做目标文件（Object File）的中间文件（或者说临时文件）。目标文件也是二进制形式的，它和可执行文件的格式是一样的。对于 Visual C++，目标文件的后缀是.obj；对于 GCC，目标文件的后缀是.o。

目标文件经过链接（Link）以后才能变成可执行文件。既然目标文件和可执行文件的格式是一样的，为什么还要再链接一次呢，直接作为可执行文件不行吗？

不行的！因为编译只是将我们自己写的代码变成了二进制形式，它还需要和系统组件（比如标准库、动态链接库等）结合起来，这些组件都是程序运行所必须的。

链接（Link）其实就是一个“打包”的过程，它将所有二进制形式的目标文件和系统组件组合成一个可执行文件。完成链接的过程也需要一个特殊的软件，叫做链接器（Linker）。

随着我们学习的深入，我们编写的代码越来越多，最终需要将它们分散到多个源文件中，编译器每次只能编译一个源文件，生成一个目标文件，这个时候，链接器除了将目标文件和系统组件组合起来，还需要将编译器生成的多个目标文件组合起来。

再次强调，编译是针对一个源文件的，有多少个源文件就需要编译多少次，就会生成多少个目标文件。 
