#第十二章 使用结构和指针
##12.1 链表
一些包含数据的独立数据结构的集合。链表中的每个节点通过链或指针连接在一起。程序通过指针访问链表中的节点。
##12.2 单链表
单链表中，每个节点包含一个指向链表下一节点的指针。
链表最后一个节点的指针字段的值为NULL。
根指针指向链表的第一个节点，根指针不包含任何数据。
单链表无法从相反的方向进行遍历，当程序到达最后一个节点时，如果想回到其他任何节点，只能从根指针从头开始。

在单链表中插入：

法一：始终保存一个指向链表当前节点之前的节点指针
遇到需要插入的节点，返回到上一节点，插入
缺点：无法在链表结尾或开头插入

优化：
在结尾处，测试是否为NULL指针
在开头处，将一个指向根指针的指针作为参数传递给函数，然后使用间接访问

进一步优化：消除根指针的特殊性
关键：链表中的每个节点都有指向它的指针，对于第一个节点，这个指针是根指针。对于其他节点，是前一个节点的指针字段。
重点在于每个节点都有一个指针指向它，至于该指针是不是在节点内部无关紧要。
因此，只需要一个指向当前节点的指针，和一个（（指向当前节点）的指针字段）的指针

##12.3 双链表
在一个双链表中，每个节点包含两个指针——指向前一个节点的指针和指向后一个节点的指针
可以以任意方式遍历双链表
为根指针声明一个完整的节点，但根指针节点的值不会被使用
根指针的fwd字段指向第一个节点，bwd字段指向最后一个节点
链表第一个节点的bwd字段和最后一个节点的fwd字段都为NULL

在双链表中插入：
当把一个节点插入到链表中，可能出现四种情况：
1.新值插入到中间位置
2.新值插入到起始位置
3.新值插入到结束位置
4.新值既插入到起始位置又插入到结束位置（原链表为空）
在每种情况下，有4个指针必须修改
1和2中，新节点的fwd指向链表的下一个节点，链表下一个节点的bwd指向新节点
3和4中，新节点的fwd设置为NULL，根节点的bwd指向新节点
1和3中，新节点的bwd指向链表的前一个节点，链表的前一个节点fwd指向新节点
2和4中，新节点的bwd设置为NULL，根节点的fwd指向新节点

简化重复代码的技巧：
1.语句提炼
if (x==3){
    i=1;
    something;
    j=2;
}else{
    i=1;
    something different;
    j=2;
}
提炼成
i=1;
if (x==3)
something;
else
something different;
j=2;
2.逻辑提炼
if (pointer!=NULL)
field=pointer;
else
field=NULL;
提炼为
field=pointer;

