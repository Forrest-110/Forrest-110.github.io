#第十一章 动态内存分配
##11.1 为什么使用动态内存分配
数组的长度往往需要在运行时知道，分配过大会浪费空间。
##11.2 malloc free
void *malloc(size_t size);
void free(void *pointer);

malloc的参数就是需要分配的内存字节数，返回一个指向被分配的内存块起始位置的指针
malloc分配的是一块连续的内存
若可用内存无法满足需求，返回null
malloc始终能满足边界对齐要求

free的参数是一个先前从malloc,calloc,realloc返回的值

#11.3 calloc realloc
void *calloc(size_t num_elements,size_t element_size);
void *realloc(void *ptrmsize_t new_size);

calloc在返回指向内存的指针之前将它初始化为0
calloc的参量包括所需元素的数量和每个元素的字节数

realloc用于修改一个原先已经分配的内存块大小，可以使一块内存扩大或缩小。
如果扩大，原先内容保留，新增加的内存添加到原先内存块后面，不初始化。
如果缩小，尾部的内存被拿掉，剩余内容仍然保留。
如果原先内存块无法改变大小，则会分配另外一块内存，并把原先内容复制到新内存上，返回新指针，不能再次使用原指针。
如果第一个参数是null，则完全等于malloc

#11.5 常见错误
1.对NULL解引用
2.对分配的内存操作时越界
3.释放并非动态分配的内存
4.释放动态分配内存的一部分（需要使用realloc缩小内存，从而释放尾部部分内存）
5.释放后继续使用

分配内存但在使用后不释放会导致内存泄漏，榨干可用内存
