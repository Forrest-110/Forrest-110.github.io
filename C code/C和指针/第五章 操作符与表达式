#操作符与表达式

##5.1 操作符
算术操作符：+ - * / % （%仅适用于整型）

移位操作符：<< >> 
逻辑移位和算术移位只有在右移且为负数时不同
无符号值执行的所有操作都是逻辑移位

位操作符：% | ^

赋值操作符：=
赋值也是表达式，具有左操作数的值
a=x=y+3;
赋值的方向从右向左
r=s+(t=u-v)/3;等价于
t=u-v;
r=s+t/3;
注：
char ch;
while ((ch=getchar())!=EOF)
有可能永远不会终止，因为getchar返回int，如果比ch大，会被截断再与EOF比较，读入\377=EOF时会被终止，在无符号字符集的机器上永远不会终止。

单目操作符
sizeof 操作数既可以是表达式也可以是变量，还可以是类型名。
sizeof 不对表达式求值，因此sizeof(a=b+1)没有赋值

自增操作符复制一份变量值的拷贝，用于周围表达式的值。前缀操作符在复制前增加变量的值，后缀运算符在复制后增加变量的值。
++a=10；是不合法的

逗号操作符
逗号操作符用于将多个表达式分隔开，这些表达式从左向右求值，整个逗号表达式的值是最后一个表达式的值
if (b+1,c/2,d>0) 如果d>0则为真

##5.3 左值和右值
左值：可以出现在赋值符号左边的，表示了一个可以存储结果值的地点
右值：可以出现在赋值符号右边的，指定了一个值

##5.4 表达式求值
long double
double
float
unsigned long int
long int
unsigned int
int
如果某个操作数的类型在上表中排名较低，则将转换为另外一个操作数的类型
整型值转换为float时可能会丧失精度，因为float仅要求6位精度

优先级和求和顺序：
两个相邻操作符的执行顺序由优先级决定。如果优先级相同，则由结合性决定。除此之外，则顺序自由。
a+b+c
a*b*c
编译器可以以任何顺序进行求值
缺点：顺序不同可能有不同结果
例如f()+g()+h()
如果函数有副作用，则调用顺序不同会有不同结果


