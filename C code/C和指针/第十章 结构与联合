#第十章 结构与联合
##10.1 结构基础知识
即使成员列表完全相同，两个结构还是被当做不同的类型

结构的自引用：
struct s{
    int a;
    struct s *b;
    int c;
}
b指针实际上指向的是同一种类型的不同结构

不完整声明：
声明一些相互之间存在依赖的结构，如果每个结构都引用了其他结构的标签。
不完整声明首先声明一个作为结构标签的标识符，然后把这个标签用在不需要知道这个结构长度的声明中，如指针。
接下来的声明把这个标签与成员列表联系在一起。
struct B;
struct A{
    struct B *partner;
    ……
};
struct B{
    struct A *partner;
    ……
}

##10.2 结构、指针与成员
访问结构成员
typedef struct{
    int a;
    short b[2];
}Ex2;
typedef struct Ex{
    int a;
    char b[3];
    Ex2 c;
    struct Ex *d;
}Ex;
Ex x={10,"Hi",{5,{-1,25}},0};
Ex *px=&x;

*px与px->a：结构的第一个成员是a，所以a的地址与结构的地址是一样的。但它们的类型不同。
创建一个指向a的指针：pi=(int*)px;
或pi=&px->a；
pi与px具有相同的值，但类型不同。

##10.3 结构的存储分配
编译器按照成员列表的顺序一个接一个给每个成员分配内存。
只有当存储成员时需要满足正确的边界对齐要求时，才有可能出现额外的内存空间。

可以对结构中的成员进行排列，使得边界要求最严的排在前面，最弱的排在后面，减少内存浪费。

如果必须确定结构中某个成员的位置，应该考虑边界对齐要素，使用offsetof宏(stddef.h)
offsetof(type,number)
返回number开始存储的位置距离结构type开始存储的位置偏移了几个字节

##10.4 作为函数参数的结构
如果直接传递结构，占据空间非常大，且要把它复制到堆栈中再丢弃。
如果传递指向结构的指针，效率能提高很多，缺陷在于会对结构修改。可以用const防止修改。

##10.5 位段
位段的声明与结构类似，但它的成员是多个位的字段。
位段的成员必须声明为int/signed int/unsigned int类型，且成员名的后面是冒号和一个整数，整数指定位段占用位的数目
struct CHAR{
    unsigned ch:7;
    unsigned font:6;
    unsigned size:19;
};
struct CHAR ch1;

##10.6 联合
联合的声明与结构类似，但联合的所有成员引用的是内存中相同的位置
联合的长度是最长成员的长度
如果想在不同时刻把不同东西存储于同一个位置，就可以使用联合。
union {
    float f;
    int i;
}fi;
fi只占据内存中一个字，如果f被使用，这个字就作为浮点值访问，如果i被使用，这个字就作为整型值被访问
fi.f=3.14;
printf("%d",fi.i);

获取联合成员的方式和获取结构成员的方式一样。唯一的差异在于，当改变一个联合成员的值时，实际上修改了该联合所有成员的值。

与结构类似，通过初始化列表（initialization list）来初始化联合对象。但是，对联合来说，列表只有一个初始化器（initializer）。
与结构一样，C99 允许在初始化器中使用成员指示符来指示哪个成员被初始化。而且，如果初始化器没有成员指示符，那么就与联合内的第一个成员关联。
