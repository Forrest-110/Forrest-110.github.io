#第七章 函数
##7.1 函数定义
函数定义就是函数体的实现。函数声明出现在函数被调用的地方。
return语句
return允许从任何位置返回。
##7.2 函数声明
如果没有关于调用函数的特定信息，编译器会假定调用的参数类型和数量是正确的，且返回一个整型值。
提供信息的方法：1、在源文件前已经出现了函数的定义，则会记住它的信息。2、函数原型。
使用原型最方便的办法：将原型置于单独的文件中，"#include" 包含该文件
如果原型和调用不匹配，会把实参和返回值转换为正确的类型。
##7.3 函数的参数
C语言的所有参数都以“传值调用”的方式传递，函数将获得参数值的一份拷贝，函数可以修改拷贝值而不会修改传递过来的实参数。
如果传递的是一个数组名，并在函数中引用数组参数，则修改数组元素会修改实际参数。这个行为叫做“传址调用”。
函数声明数组参数时可以不指定数组长度，因为并不分配空间。
##7.4 ADT和黑盒
ADT：抽象数据类型。模块具有功能说明和接口说明，前者说明模块所执行的任务，后者定义模块的使用。用户不需要知道模块实现的细节，除了接口，用户不能访问模块。
限制对模块的访问是通过static实现的。
##7.5 递归
C语言通过运行时堆栈支持递归函数的实现。
例：递归实现整型值转换为字符
void binart_to_ascii(unsigned int value){
    unsigned int quotient;
    quotient=value/10;
    if (quotient!=0)
    binart_to_ascii(quotient);
    putchar(value%10+'0');
}
追踪递归函数执行的关键是理解函数中所声明的变量是如何储存的。函数被调用时，
它的变量的空间是创建于运行时堆栈上的。以前调用的函数的变量仍保留在堆栈上，
但它们被新函数的变量所覆盖，因此不能被访问。递归函数每进行一次新的调用，
都会创建新的变量覆盖前一次调用所创建的变量。

递归函数调用将涉及运行时开销：参数必须压到堆栈中，为局部变量分配空间，寄存器的值必须保存。
每次调用返回时，上述这些操作必须被还原。

##7.6 可变参数列表
stdarg宏：stdarg.h定义了一个类型va_list，3个宏：va_start,va_arg,va_end。
两个限制：
1.这些宏无法判断实际存在的参数的数量
2.这些宏无法判断每个参数的类型
